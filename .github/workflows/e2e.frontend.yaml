name: e2e:frontend
on:
  pull_request:
    # paths:
    #   - 'frontend/**'
# If two events are triggered within a short time in the same PR, cancel the run of the oldest event
concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      matrix:
        test:
          - enterprise/feature.spec.ts
          - enterprise/groups.spec.ts
          - enterprise/projects/access.spec.ts
          - oss/segments/segments.spec.ts
          - oss/feature/feature.spec.ts
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Start backend services
        run: docker compose -f .github/docker-compose.test.yml up -d db unleash
        env:
          FRONTEND_TEST_LICENSE: ${{ secrets.FRONTEND_TEST_LICENSE }}
      - uses: pnpm/action-setup@v5
      - name: Use Node.js 22.x
        uses: actions/setup-node@v6
        with:
          node-version: 22.x
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml
      - name: Cache Cypress binary
        uses: actions/cache@v5
        with:
          path: ~/.cache/Cypress
          key: cypress-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
      - name: Cache frontend build
        id: cache-frontend-build
        uses: actions/cache@v5
        with:
          path: frontend/build
          key: frontend-build-${{ runner.os }}-${{ hashFiles('frontend/src/**', 'frontend/public/**', 'frontend/index.html', 'frontend/vite.config.ts', 'frontend/tsconfig.json', 'pnpm-lock.yaml') }}
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Build static frontend
        if: steps.cache-frontend-build.outputs.cache-hit != 'true'
        run: pnpm --dir frontend build
      - name: Start frontend service and wait for all services
        run: docker compose -f .github/docker-compose.test.yml up -d --wait -t 90 frontend
        env:
          FRONTEND_TEST_LICENSE: ${{ secrets.FRONTEND_TEST_LICENSE }}
      - name: Run Cypress
        uses: cypress-io/github-action@v6
        with:
          working-directory: frontend
          env: AUTH_USER=admin,AUTH_PASSWORD=unleash4all
          config: baseUrl=http://localhost:3000
          spec: cypress/${{ matrix.test }}
          install: false
      - name: Set artifact name
        if: always()
        id: artifact-name
        run: echo "name=cypress-artifacts-$(echo '${{ matrix.test }}' | tr '/' '-')" >> $GITHUB_OUTPUT
      - name: Upload Cypress artifacts
        uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: ${{ steps.artifact-name.outputs.name }}
          path: |
            frontend/cypress/screenshots
            frontend/cypress/videos
          if-no-files-found: ignore
          retention-days: 3
