`const $name = $init` as $decl where {
    $init <: or {
        `styled($_)($_)`,
        `styled($_)<$_>($_)`,
        `styled($_, $_)($_)`,
        `styled($_, $_)<$_>($_)`,
        `styled.$_($_)`,
        `styled.$_<$_>($_)`,
        `styled($_)`,
        `styled.$_`
    },
    ! $name <: r"_?Styled.*",
    ! $decl <: within `export const $_ = $_` ,
    register_diagnostic(span=$name, message="Components created with styled() should be named with a 'Styled' prefix.", severity="info")
}
