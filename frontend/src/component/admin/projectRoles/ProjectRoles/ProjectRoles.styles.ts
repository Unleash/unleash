import { makeStyles } from 'tss-react/mui';

export const useStyles = makeStyles()(theme => ({
    rolesListBody: {
        paddingBottom: '4rem',
        minHeight: '50vh',
        position: 'relative',
    },
}));
