import { styled, Typography } from '@mui/material';

export const ChangeItemInfo = styled(Typography)(({ theme }) => ({
    display: 'flex',
    flexFlow: 'row',
    alignItems: 'center',
    flex: 'auto',
    gap: `1ch`,
}));
