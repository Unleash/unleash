import { makeStyles } from 'tss-react/mui';

export const useStyles = makeStyles()({
    proclamation: {
        marginBottom: '1rem',
    },
    content: {
        maxWidth: '800px',
    },
    link: {
        display: 'block',
        marginTop: '0.5rem',
        width: '100px',
    },
});
