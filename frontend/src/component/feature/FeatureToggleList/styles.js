import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles({
    actionsContainer: {
        display: 'flex',
        alignItems: 'center',
    },
    listParagraph: {
        textAlign: 'center',
    },
    searchBarContainer: {
        marginBottom: '2rem',
    },
});
