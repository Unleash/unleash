import { makeStyles } from 'tss-react/mui';

export const useStyles = makeStyles()(() => ({
    iconButton: {
        marginLeft: '0.5rem',
    },
    actionList: {
        display: 'flex',
        alignItems: 'center',
    },
}));
