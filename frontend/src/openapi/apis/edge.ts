/**
 * Generated by Orval
 * Do not edit manually.
 * See `gen:api` script in package.json
 */
import type {
    ValidatedEdgeTokensSchema,
    TokenStringListSchema,
    BulkMetricsSchema,
} from '../models';
import { fetcher } from '../fetcher';
import type { BodyType } from '../fetcher';

/**
 * This operation accepts a list of tokens to validate. Unleash will validate each token you provide. For each valid token you provide, Unleash will return the token along with its type and which projects it has access to.
 * @summary Check which tokens are valid
 */
export const getValidTokens = (
    tokenStringListSchema: BodyType<TokenStringListSchema>
) => {
    return fetcher<ValidatedEdgeTokensSchema>({
        url: `/edge/validate`,
        method: 'post',
        headers: { 'Content-Type': 'application/json' },
        data: tokenStringListSchema,
    });
};

/**
 * This operation accepts batched metrics from Edge. Metrics will be inserted into Unleash's metrics storage
 * @summary Send metrics from Edge
 */
export const bulkMetrics = (bulkMetricsSchema: BodyType<BulkMetricsSchema>) => {
    return fetcher<void>({
        url: `/edge/metrics`,
        method: 'post',
        headers: { 'Content-Type': 'application/json' },
        data: bulkMetricsSchema,
    });
};
