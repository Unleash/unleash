/**
 * Generated by Orval
 * Do not edit manually.
 * See `gen:api` script in package.json
 */
import type { UserAccessLogEntrySchemaPerformedBy } from './userAccessLogEntrySchemaPerformedBy';
import type { UserAccessLogEntrySchemaStatus } from './userAccessLogEntrySchemaStatus';
import type { UserAccessLogEntrySchemaUser } from './userAccessLogEntrySchemaUser';

/**
 * A single user access log entry: when a user was added to and/or removed from the Unleash instance, the role they held, and who performed the action.
 */
export interface UserAccessLogEntrySchema {
    /** When the user was added to the instance. */
    createdAt?: string;
    /** The user who performed the most recent action on this user (the removal if removed, otherwise the creation). Omitted when the actor is unknown. */
    performedBy?: UserAccessLogEntrySchemaPerformedBy;
    /** When the user was removed from the instance. Omitted if the user is still active. */
    removedAt?: string;
    /** Whether the user is currently added to the instance, or has been removed. */
    status: UserAccessLogEntrySchemaStatus;
    /** The user this entry describes. Mirrors the identity fields of an Unleash user so it can be rendered like any other user reference. */
    user: UserAccessLogEntrySchemaUser;
}
