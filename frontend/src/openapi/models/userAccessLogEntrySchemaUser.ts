/**
 * Generated by Orval
 * Do not edit manually.
 * See `gen:api` script in package.json
 */

/**
 * The user this entry describes. Mirrors the identity fields of an Unleash user so it can be rendered like any other user reference.
 */
export type UserAccessLogEntrySchemaUser = {
    /** The email of the user. */
    email?: string;
    /** The id of the user. */
    id: number;
    /** The URL of the user profile image. Only available while the user still exists. */
    imageUrl?: string;
    /** The name of the user. */
    name?: string;
    /** The id of the user root role — their current role for active users, or the role they held at removal for removed users. Resolve it against the `rootRoles` list on the response. */
    rootRole?: number;
    /** The username of the user. */
    username?: string;
};
