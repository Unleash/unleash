/**
 * Generated by Orval
 * Do not edit manually.
 * See `gen:api` script in package.json
 */
import type { UserSessionSchemaDeviceType } from './userSessionSchemaDeviceType';

/**
 * A model representing a single active user session.
 */
export interface UserSessionSchema {
    /**
     * The browser the session was started from, derived from the User-Agent. May be null when unknown or not captured.
     * @nullable
     */
    browser?: string | null;
    /** When the session was created. */
    createdAt: string;
    /** Whether this is the session of the user making the request. Revoking it will log the caller out. */
    current?: boolean;
    /**
     * The high-level device type the session was started from, derived from the User-Agent. May be null when unknown or not captured.
     * @nullable
     */
    deviceType?: UserSessionSchemaDeviceType;
    /**
     * The email of the user, if known.
     * @nullable
     */
    email?: string | null;
    /**
     * When the session expires, if known.
     * @nullable
     */
    expiredAt?: string | null;
    /** An opaque, non-reversible identifier for the session. Use this value to revoke the session; the underlying session id is never exposed. */
    id: string;
    /**
     * The IP address the session was first seen from. May be null for sessions created before IP capture was enabled.
     * @nullable
     */
    ip?: string | null;
    /**
     * The id of the user the session belongs to.
     * @nullable
     */
    userId?: number | null;
    /**
     * The username or email of the user.
     * @nullable
     */
    username?: string | null;
}
