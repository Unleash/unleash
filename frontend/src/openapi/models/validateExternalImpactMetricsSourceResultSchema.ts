/**
 * Generated by Orval
 * Do not edit manually.
 * See `gen:api` script in package.json
 */

/**
 * Result of validating an external impact-metrics source URL. The endpoint always returns 200; failures are reported in the `error` field while `metrics` is empty.
 */
export interface ValidateExternalImpactMetricsSourceResultSchema {
    /** Human-readable explanation of why the URL could not be validated. Omitted on success. */
    error?: string;
    /** List of metric names discovered at the supplied URL. Empty when the URL could not be validated. */
    metrics: string[];
}
