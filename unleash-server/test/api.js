var assert = require('assert');

describe('The api', function () {
    beforeEach(function () {
        //
    });

    it('returns all toggles', function (done) {
        done();
        assert(true);
    });
});